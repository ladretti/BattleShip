# Plan d'implémentation — Le rendu 3D réactif

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un arrière-plan 3D réactif derrière le jeu, alimenté uniquement par ce que le joueur a le droit de savoir.

**Architecture:** La question « quels navires adverses sont coulés à l'instant *n* » devient **une** fonction pure dans `BattleShip.Models`, testée, et consommée par les deux rendus — le DOM existant et la scène 3D. Le canvas est décoratif (`aria-hidden`, sans pointeur) ; le jeu reste le DOM. Three.js est vendorisé, piloté par un objet d'interop calqué sur le `battleshipSettings` existant.

**Tech Stack:** .NET 10, Blazor WebAssembly, xUnit, Three.js (vendorisé), WebGL.

**Spec:** `docs/superpowers/specs/2026-09-22-rendu-3d-design.md`

## Global Constraints

- **Langue** : code en **anglais** (identifiants, littéraux, texte affiché) ; documentation humaine et **messages de commit** en **français**.
- **Aucun commentaire dans le code** — ni `//`, ni `/* */`, ni documentation XML. Décision du binôme. Exception de fait : `wwwroot/index.html` en contient déjà, on suit le style local **du fichier** sans en ajouter ailleurs.
- **Nommage** : `PascalCase` types/méthodes/propriétés ; `camelCase` paramètres/locales ; `_camelCase` champs privés ; suffixe `Async` sur tout `Task`/`Task<T>`.
- **Noms de tests** : anglais descriptif, underscores, style `A_shot_outside_the_grid_is_rejected`.
- **Nullable activé** ; `sealed` par défaut ; aucun `.Result` ni `.Wait()`.
- **`dotnet build` à ZÉRO avertissement** ; `dotnet format` avant chaque commit.
- **Le secret** : aucune position d'un navire adverse non coulé ne doit atteindre le front — canvas compris. Le graphe de scène est lisible depuis la console.
- **Le jeu reste le DOM** : canvas `aria-hidden`, `pointer-events: none`, `z-index: -1`.
- **Aucun nouveau projet de tests.** Le total actuel est **169** ; il doit monter, jamais descendre.
- Message de commit terminé par : `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

### Ligne de base mesurée le 2026-09-22

```
dotnet build   → 0 avertissement, 0 erreur
dotnet test    → 169/169
```

---

## Structure des fichiers

| fichier | responsabilité |
|---|---|
| `BattleShip.Models/Scene/SceneState.cs` | **créé** — les DTO `SceneShip` / `SceneState` |
| `BattleShip.Models/Scene/ScenePlan.cs` | **créé** — `For(game, events, cursor)`, la fonction pure et testée |
| `BattleShip.Tests/Domain/ScenePlanTests.cs` | **créé** — les quatre tests du § 5 de la spec |
| `BattleShip.App/Pages/Play.razor` | **modifié** — rebranché sur `ScenePlan`, perd trois méthodes privées |
| `BattleShip.App/wwwroot/lib/three.module.js` | **créé** — Three.js vendorisé |
| `BattleShip.App/wwwroot/js/scene.js` | **créé** — `battleshipScene` : `init` / `update` / `dispose` |
| `BattleShip.App/wwwroot/index.html` | **modifié** — charge le module de scène |
| `BattleShip.App/Components/SceneCanvas.razor` | **créé** — le composant, `IAsyncDisposable` |
| `BattleShip.App/wwwroot/css/app.css` | **modifié** — positionnement du canvas |
| `docs/adr/0012-rendu-3d.md` | **créé** — **avant** tout code |

---

## Tâche 0 : l'ADR, avant la première ligne de code

Le `CLAUDE.md` § 3 bis ferme la liste des patterns : tout ajout se décide en ADR **avant** d'être codé. Une dépendance front est un ajout structurant.

**Files:**
- Create: `docs/adr/0012-rendu-3d.md`

**Interfaces:**
- Consumes: la spec `docs/superpowers/specs/2026-09-22-rendu-3d-design.md`
- Produces: rien de codé

- [ ] **Étape 1 : lire le gabarit et deux ADR voisins**

Run: `cat docs/adr/0001-modele.md docs/adr/0009-trois-skins.md docs/adr/0010-coques.md`

Gabarit à six sections : `Statut et date`, `Contexte`, `Options envisagées`, `Décision`, `Conséquences`, `Vérification et réexamen`, `Références`.

- [ ] **Étape 2 : écrire l'ADR**

Contenu obligatoire — ce sont les objections attendues à l'oral :

1. **Pourquoi une dépendance alors que le projet n'en avait aucune côté front.** Three.js est vendorisé, pas en CDN : le README documente déjà une dégradation hors-ligne (Google Fonts), on n'en ajoute pas une seconde.
2. **Pourquoi le canvas est décoratif et non le jeu.** Un `<canvas>` n'a pas de DOM : la navigation clavier, la région `aria-live`, les glyphes par état et les contrastes mesurés (tâche 21) devraient être réimplémentés en double. Le canvas décoratif ne coûte aucune de ces garanties.
3. **Le secret dans un graphe de scène.** `scene.children` se lit depuis la console. Des coques adverses invisibles seraient une fuite qu'aucun test d'intégration ne verrait. Filtrage à la source, jamais à l'affichage.
4. **Pourquoi la projection descend dans `Models`** — établi par spike, pas supposé : `BattleShip.Tests` ne peut pas référencer `App` (`CS0433`, `BattleService` généré des deux côtés depuis le même `.proto`). Coller la sortie du spike.
5. **Pourquoi `Play.razor` est rebranché dessus.** Deux implémentations du même calcul critique pour le secret dériveraient ; le chantier précédent l'a montré (R18). C'est une suppression de duplication, pas un ajout.
6. **Rapport aux ADR existants** : 0009 (apparences, réglage de mouvement) — on s'y branche ; 0010 (coques en couche superposée) — même principe, autre médium ; ni l'un ni l'autre n'est remplacé.
7. **Vérification et réexamen** : renvoyer aux quatre tests du § 5 de la spec ; condition de réexamen — si le canvas devenait interactif, l'arbitrage d'accessibilité serait à refaire entièrement.

- [ ] **Étape 3 : commit**

```bash
dotnet format
git add docs/adr/0012-rendu-3d.md
git commit -m "docs: ADR 0012 — un arrière-plan 3D décoratif, pas un jeu en 3D

Décidé avant tout code. Documente pourquoi le canvas reste décoratif
(le coût en accessibilité d'un jeu en canvas), pourquoi la dépendance
est vendorisée, et pourquoi la projection descend dans Models — le
spike CS0433 est cité.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 1 : la projection, pure et testée

Le cœur du chantier. Tout le reste est du dessin.

**Files:**
- Create: `BattleShip.Models/Scene/SceneState.cs`
- Create: `BattleShip.Models/Scene/ScenePlan.cs`
- Test: `BattleShip.Tests/Domain/ScenePlanTests.cs`

**Interfaces:**
- Consumes: `GameDto`, `OwnBoardDto`, `OpponentBoardDto`, `SunkShipDto`, `CellDto`, `GameEvent`, `ShotFired`, `GameFold.Fold`, `Coordinate`, `Player`, `ShotResult` (tous existants dans `BattleShip.Models`)
- Produces:
  - `public sealed record SceneShip(string Name, int X, int Y, int Size, bool Vertical, bool Sunk)`
  - `public sealed record SceneState(int GridSize, IReadOnlyList<SceneShip> Friendly, IReadOnlyList<SceneShip> Wrecks, Coordinate? LastImpact, bool Revealed)`
  - `public static SceneState ScenePlan.For(Guid id, GameDto game, IReadOnlyList<GameEvent> events, int? cursor, Coordinate? lastImpact)`

- [ ] **Étape 1 : écrire les tests qui échouent**

```csharp
using BattleShip.Models;
using BattleShip.Models.Contracts;

namespace BattleShip.Tests.Domain;

public sealed class ScenePlanTests
{
    private static (Guid Id, GameDto Dto, IReadOnlyList<GameEvent> Events, Game Game) Played(int seed, int shots)
    {
        var rules = GameRules.Default;
        var human = new FleetPlacer(new Random(seed)).PlaceAll(rules).Value;
        var opponent = new FleetPlacer(new Random(seed + 1)).PlaceAll(rules).Value;
        var game = Game.Start(Guid.NewGuid(), rules, human, opponent);

        var fired = 0;
        foreach (var cell in opponent.SelectMany(s => s.Cells).ToList())
        {
            if (fired >= shots || game.Status == GameStatus.Finished)
                break;

            if (game.CurrentPlayer != Player.Human)
            {
                var free = Enumerable.Range(0, rules.GridSize * rules.GridSize)
                    .Select(i => new Coordinate(i % rules.GridSize, i / rules.GridSize))
                    .First(c => !game.HumanBoard.ReceivedShots.Contains(c));
                Assert.True(game.OpponentFires(free).IsOk);
                continue;
            }

            Assert.True(game.PlayerFires(cell).IsOk);
            fired++;
        }

        return (game.Id, DtoFor(game), game.Events, game);
    }

    private static GameDto DtoFor(Game game) =>
        new(game.Id,
            game.Status.ToString(),
            game.CurrentPlayer.ToString(),
            new OwnBoardDto(
                game.Rules.GridSize,
                [.. game.HumanBoard.Ships.Select(s => new ShipDto(
                    s.Name, s.Size,
                    [.. s.Cells.Select(c => new CellDto(c.X, c.Y, s.IsSunk ? "sunk" : "ship"))],
                    s.IsSunk))],
                []),
            new OpponentBoardDto(
                game.Rules.GridSize,
                [],
                [.. game.OpponentBoard.Ships.Where(s => s.IsSunk)
                    .Select(s => new SunkShipDto(
                        s.Name,
                        [.. s.Cells.Select(c => new CellDto(c.X, c.Y, "sunk"))]))]),
            game.OpponentDifficulty,
            [.. game.Rules.Fleet.Select(t => new ShipTemplateDto(t.Name, t.Size))],
            game.Rules.ShipsMayTouch,
            game.Winner?.ToString());

    [Fact]
    public void During_a_game_the_wrecks_are_only_sunk_ships()
    {
        var (id, dto, events, game) = Played(seed: 41, shots: 6);
        Assert.NotEqual(GameStatus.Finished, game.Status);

        var scene = ScenePlan.For(id, dto, events, cursor: null, lastImpact: null);

        Assert.All(scene.Wrecks, w => Assert.True(w.Sunk));
        Assert.False(scene.Revealed);
    }

    [Fact]
    public void During_a_game_no_unsunk_opposing_cell_reaches_the_scene()
    {
        var (id, dto, events, game) = Played(seed: 41, shots: 6);
        Assert.NotEqual(GameStatus.Finished, game.Status);

        var scene = ScenePlan.For(id, dto, events, cursor: null, lastImpact: null);

        var exposed = scene.Friendly.Concat(scene.Wrecks)
            .SelectMany(Footprint)
            .ToHashSet();

        var revealed = game.OpponentBoard.ReceivedShots;
        var secret = game.OpponentBoard.Ships
            .SelectMany(s => s.Cells)
            .Where(c => !revealed.Contains(c))
            .ToList();

        Assert.NotEmpty(secret);
        Assert.Empty(exposed.Intersect(secret));
    }

    [Fact]
    public void After_the_game_ends_the_whole_opposing_fleet_is_revealed()
    {
        var (id, dto, events, game) = Played(seed: 41, shots: 500);
        Assert.Equal(GameStatus.Finished, game.Status);

        var scene = ScenePlan.For(id, dto, events, cursor: null, lastImpact: null);

        Assert.True(scene.Revealed);
        Assert.Equal(
            game.OpponentBoard.Ships.Count,
            scene.Wrecks.Count);
    }

    [Fact]
    public void At_cursor_n_the_wrecks_are_those_sunk_at_that_instant()
    {
        var (id, dto, events, game) = Played(seed: 41, shots: 500);

        var sinking = game.Events.OfType<ShotFired>()
            .First(e => e.By == Player.Human && e.Result == ShotResult.Sunk);

        var shotsBefore = game.Events.OfType<ShotFired>()
            .TakeWhile(e => e.Sequence < sinking.Sequence)
            .Count();

        var before = ScenePlan.For(id, dto, events, cursor: shotsBefore, lastImpact: null);
        var after = ScenePlan.For(id, dto, events, cursor: shotsBefore + 1, lastImpact: null);

        Assert.DoesNotContain(before.Wrecks, w => w.Name == sinking.SunkShipName);
        Assert.Contains(after.Wrecks, w => w.Name == sinking.SunkShipName);
    }

    [Fact]
    public void On_a_censored_journal_the_wreck_appears_at_the_shot_that_sank_it()
    {
        var (id, dto, events, _) = Played(seed: 41, shots: 500);

        var censored = Censored(events);

        var sinking = events.OfType<ShotFired>()
            .First(e => e.By == Player.Human && e.Result == ShotResult.Sunk);

        var shotsBefore = events.OfType<ShotFired>()
            .TakeWhile(e => e.Sequence < sinking.Sequence)
            .Count();

        var before = ScenePlan.For(id, dto, censored, shotsBefore, lastImpact: null);
        var after = ScenePlan.For(id, dto, censored, shotsBefore + 1, lastImpact: null);

        Assert.DoesNotContain(before.Wrecks, w => w.Name == sinking.SunkShipName);
        Assert.Contains(after.Wrecks, w => w.Name == sinking.SunkShipName);
    }

    private static IReadOnlyList<GameEvent> Censored(IReadOnlyList<GameEvent> events) =>
    [
        .. events.Select(e => e is GameCreated created
            ? new GameCreated(created.Sequence, created.Rules, [], created.Difficulty)
            : e)
    ];

    private static IEnumerable<Coordinate> Footprint(SceneShip ship) =>
        Enumerable.Range(0, ship.Size)
            .Select(i => ship.Vertical
                ? new Coordinate(ship.X, ship.Y + i)
                : new Coordinate(ship.X + i, ship.Y));
}
```

**Le cinquième test est le plus important des cinq, et il a été ajouté au scan de pré-vol.**
Les quatre premiers utilisent `game.Events`, c'est-à-dire le journal **serveur complet**, dont
`GameCreated` porte les positions adverses — `WrecksAt` prend donc toujours la branche « journal
révélé ». Or en production le front reçoit un journal **censuré** : c'est la branche
`sunkByCursor` qui s'exécute, et c'est exactement là que vivait la régression R18 de l'axe A.
`Censored` reproduit ce que `EventProjection` sert réellement — `GameCreated` avec
`OpponentShips` vide — pour que le chemin de production soit celui qu'on teste.

Les tests 1 et 3 forment une **paire** : le premier seul est satisfait par une fonction qui ne rend jamais rien, le second seul par une fonction qui ne censure jamais.

Le test 4 est le pendant de la régression R18 de l'axe A : c'est le seul qui distingue « les épaves à l'instant *n* » de « les épaves maintenant ».

**Règle de fixture apprise sur le chantier précédent** : la fabrique `Played` asserte `IsOk` sur **chaque** tir. Un `Result` refusé en silence (`NotYourTurn`) produirait une fixture dégénérée qui fait passer les tests sur du sable.

- [ ] **Étape 2 : exécuter pour vérifier que ça échoue**

Run: `dotnet test --filter "FullyQualifiedName~ScenePlanTests"`
Expected: **échec de compilation** — `ScenePlan` et `SceneState` n'existent pas.

- [ ] **Étape 3 : écrire les DTO**

`BattleShip.Models/Scene/SceneState.cs` :

```csharp
namespace BattleShip.Models;

public sealed record SceneShip(string Name, int X, int Y, int Size, bool Vertical, bool Sunk);

public sealed record SceneState(
    int GridSize,
    IReadOnlyList<SceneShip> Friendly,
    IReadOnlyList<SceneShip> Wrecks,
    Coordinate? LastImpact,
    bool Revealed);
```

- [ ] **Étape 4 : écrire la projection**

`BattleShip.Models/Scene/ScenePlan.cs` :

```csharp
using BattleShip.Models.Contracts;

namespace BattleShip.Models;

public static class ScenePlan
{
    public static SceneState For(
        Guid id,
        GameDto game,
        IReadOnlyList<GameEvent> events,
        int? cursor,
        Coordinate? lastImpact)
    {
        var friendly = game.Own.Ships.Select(ToSceneShip).OfType<SceneShip>().ToList();

        var wrecks = cursor is { } at
            ? WrecksAt(id, game, events, at)
            : game.Opponent.SunkShips
                .Select(s => ToSceneShip(s.Name, s.Cells, sunk: true))
                .OfType<SceneShip>()
                .ToList();

        var revealed = game.Status == nameof(GameStatus.Finished);

        return new SceneState(game.Own.GridSize, friendly, wrecks, lastImpact, revealed);
    }

    private static List<SceneShip> WrecksAt(
        Guid id, GameDto game, IReadOnlyList<GameEvent> events, int cursor)
    {
        var upTo = Prefix(events, cursor);
        var folded = upTo.Count == 0 ? null : GameFold.Fold(id, upTo);

        if (folded is { } replayed && replayed.OpponentBoard.Ships.Count > 0)
        {
            return [.. replayed.OpponentBoard.Ships
                .Where(s => s.IsSunk)
                .Select(s => ToSceneShip(s.Name, [.. s.Cells.Select(c => new CellDto(c.X, c.Y, "sunk"))], sunk: true))
                .OfType<SceneShip>()];
        }

        var sunkByCursor = upTo.OfType<ShotFired>()
            .Where(shot => shot.By == Player.Human && shot.Result == ShotResult.Sunk)
            .Select(shot => shot.SunkShipName!)
            .ToHashSet();

        return [.. game.Opponent.SunkShips
            .Where(ship => sunkByCursor.Contains(ship.Name))
            .Select(ship => ToSceneShip(ship.Name, ship.Cells, sunk: true))
            .OfType<SceneShip>()];
    }

    private static List<GameEvent> Prefix(IReadOnlyList<GameEvent> events, int cursor)
    {
        var shots = 0;
        var upTo = new List<GameEvent>();

        foreach (var next in events)
        {
            if (next is ShotFired && shots++ >= cursor)
                break;

            upTo.Add(next);
        }

        return upTo;
    }

    private static SceneShip? ToSceneShip(ShipDto ship) =>
        ToSceneShip(ship.Name, ship.Cells, ship.IsSunk);

    private static SceneShip? ToSceneShip(string name, IReadOnlyList<CellDto> cells, bool sunk)
    {
        if (cells.Count == 0)
            return null;

        var sameRow = cells.All(c => c.Y == cells[0].Y);
        var sameColumn = cells.All(c => c.X == cells[0].X);

        if (!sameRow && !sameColumn)
            return null;

        return new SceneShip(
            name,
            cells.Min(c => c.X),
            cells.Min(c => c.Y),
            cells.Count,
            !sameRow && sameColumn,
            sunk);
    }
}
```

**Le point délicat** : `Prefix` compte les `ShotFired` **tous joueurs confondus**, exactement comme `ReplayedGame` dans `Play.razor`. Un comptage différent afficherait une épave un coup trop tôt ou trop tard. C'est la même cohérence que la re-relecture de la tâche 8 de l'axe A avait vérifiée à la main.

**Second point** : `GameFold.Fold` **lève** sur un journal vide (`ArgumentException`) — d'où la garde `upTo.Count == 0`.

- [ ] **Étape 5 : exécuter pour vérifier que ça passe**

Run: `dotnet test --filter "FullyQualifiedName~ScenePlanTests"`
Expected: **PASS**, 5 tests.

Run: `dotnet test`
Expected: **174/174** (169 + 5). Aucun test existant ne casse — rien d'existant n'a été touché.

- [ ] **Étape 6 : commit**

```bash
dotnet format
git add BattleShip.Models/Scene/ BattleShip.Tests/Domain/ScenePlanTests.cs
git commit -m "feat: une projection pure et testée pour la scène 3D

ScenePlan répond à « quels navires adverses sont coulés à l'instant n »
sans dépendre de Blazor, donc BattleShip.Tests peut l'exercer — ce que
BattleShip.App ne permettait pas (CS0433 : BattleService généré des deux
côtés depuis le même .proto).

Les tests 1 et 3 encadrent la censure : chacun seul est satisfait par une
implémentation fausse. Le test 4 est le pendant de la régression R18 —
les épaves à l'instant n, pas les épaves maintenant.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 2 : `Play.razor` consomme la projection partagée

Suppression de duplication. Aucun comportement visible ne change.

**Files:**
- Modify: `BattleShip.App/Pages/Play.razor`

**Interfaces:**
- Consumes: `ScenePlan.For` (tâche 1)
- Produces: rien de nouveau ; `OpponentHulls` garde sa signature

- [ ] **Étape 1 : constater l'équivalence avant de toucher**

Run: `grep -n "LiveOpponentHulls\|RevealedOpponentHulls\|CensoredOpponentHulls\|OpponentHulls" BattleShip.App/Pages/Play.razor`

Les trois méthodes privées répondent à la même question que `ScenePlan.WrecksAt`. Noter les numéros de ligne : elles vont disparaître.

- [ ] **Étape 2 : réécrire `OpponentHulls`**

Remplacer les quatre méthodes (`OpponentHulls`, `LiveOpponentHulls`, `RevealedOpponentHulls`, `CensoredOpponentHulls`) par une seule :

```csharp
    private IReadOnlyList<ShipOutline> OpponentHulls(GameDto game) =>
    [
        .. ScenePlan.For(game.Id, game, _events, _replayCursor, lastImpact: null).Wrecks
            .Select(w => ShipOutline.FromCells(
                w.Name,
                [.. Footprint(w).Select(c => new CellDto(c.X, c.Y, "sunk"))],
                isSunk: true))
            .OfType<ShipOutline>()
    ];

    private static IEnumerable<Coordinate> Footprint(SceneShip ship) =>
        Enumerable.Range(0, ship.Size)
            .Select(i => ship.Vertical
                ? new Coordinate(ship.X, ship.Y + i)
                : new Coordinate(ship.X + i, ship.Y));
```

`ReplayedGame` reste : `OwnCells` et `OwnHulls` s'en servent encore.

- [ ] **Étape 3 : vérifier que rien ne bouge**

Run: `dotnet build`
Expected: **0 avertissement**.

Run: `dotnet test`
Expected: **174/174**, inchangé.

Le vrai contrôle est visuel et vient à la tâche 6 — aucun test ne couvre `Play.razor`. Noter dans le rapport que cette tâche est établie par **lecture**, pas par test.

- [ ] **Étape 4 : commit**

```bash
dotnet format
git add BattleShip.App/Pages/Play.razor
git commit -m "refactor: Play.razor consomme la projection partagée

Trois méthodes privées disparaissent au profit de ScenePlan. Deux
implémentations du même calcul critique pour le secret auraient dérivé —
c'est précisément ce que la régression R18 a coûté au chantier précédent.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 3 : Three.js vendorisé et l'objet d'interop

**Files:**
- Create: `BattleShip.App/wwwroot/lib/three.module.js`
- Create: `BattleShip.App/wwwroot/js/scene.js`
- Modify: `BattleShip.App/wwwroot/index.html`

**Interfaces:**
- Consumes: rien côté C#
- Produces: l'objet global `battleshipScene` avec `init(canvas) -> bool`, `update(state)`, `dispose()`

- [ ] **Étape 1 : vendoriser Three.js**

Télécharger le build ESM de Three.js dans `BattleShip.App/wwwroot/lib/three.module.js`.

```bash
mkdir -p BattleShip.App/wwwroot/lib
curl -L -o BattleShip.App/wwwroot/lib/three.module.js \
  https://unpkg.com/three@0.169.0/build/three.module.js
ls -lh BattleShip.App/wwwroot/lib/three.module.js
```

**Pas de CDN à l'exécution** : le fichier est servi par le front. Le README documente déjà une dégradation hors-ligne (Google Fonts), on n'en ajoute pas une seconde.

Si `curl` échoue (pas de réseau), **arrêter et le signaler** — ne pas inventer un repli.

- [ ] **Étape 2 : lire le motif d'interop existant**

Run: `sed -n '20,45p' BattleShip.App/wwwroot/index.html`

L'objet `battleshipSettings` y expose `read` / `write` / `remove` / `prefersReducedMotion`, chacun avec son `try`/`catch`. Le nouvel objet suit **la même forme** : on n'introduit pas une seconde manière de parler à JavaScript.

- [ ] **Étape 3 : écrire `scene.js`**

```js
import * as THREE from '../lib/three.module.js';

const state = { renderer: null, scene: null, camera: null, sea: null, ships: [], raf: 0, frozen: false };

function disposeShips() {
    for (const mesh of state.ships) {
        state.scene.remove(mesh);
        mesh.geometry.dispose();
        mesh.material.dispose();
    }
    state.ships = [];
}

window.battleshipScene = {
    init(canvas) {
        try {
            state.renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
        } catch (e) {
            return false;
        }
        if (!state.renderer) return false;

        state.renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
        state.scene = new THREE.Scene();
        state.camera = new THREE.PerspectiveCamera(45, 1, 0.1, 500);
        state.camera.position.set(0, 26, 34);
        state.camera.lookAt(0, 0, 0);

        state.scene.add(new THREE.HemisphereLight(0x9fc6d8, 0x0a1a24, 1.1));
        const sun = new THREE.DirectionalLight(0xffffff, 0.8);
        sun.position.set(12, 30, 18);
        state.scene.add(sun);

        const seaGeometry = new THREE.PlaneGeometry(200, 200, 96, 96);
        seaGeometry.rotateX(-Math.PI / 2);
        state.sea = new THREE.Mesh(
            seaGeometry,
            new THREE.MeshStandardMaterial({ color: 0x14384a, roughness: 0.75, metalness: 0.1 })
        );
        state.scene.add(state.sea);

        const resize = () => {
            const w = canvas.clientWidth || 1;
            const h = canvas.clientHeight || 1;
            state.renderer.setSize(w, h, false);
            state.camera.aspect = w / h;
            state.camera.updateProjectionMatrix();
        };
        resize();
        window.addEventListener('resize', resize);

        const base = seaGeometry.attributes.position.array.slice();
        const tick = (t) => {
            state.raf = requestAnimationFrame(tick);
            if (!state.frozen) {
                const p = seaGeometry.attributes.position;
                for (let i = 0; i < p.count; i++) {
                    const x = base[i * 3], z = base[i * 3 + 2];
                    p.array[i * 3 + 1] = Math.sin((x + t * 0.0012) * 0.18) * 0.5
                                       + Math.cos((z + t * 0.0009) * 0.22) * 0.4;
                }
                p.needsUpdate = true;
                seaGeometry.computeVertexNormals();
            }
            state.renderer.render(state.scene, state.camera);
        };
        state.raf = requestAnimationFrame(tick);
        return true;
    },

    update(scene) {
        if (!state.renderer) return;
        disposeShips();

        const half = scene.gridSize / 2;
        const place = (s, color, sunk) => {
            const long = s.size;
            const geometry = new THREE.BoxGeometry(
                s.vertical ? 0.7 : long * 0.9, 0.55, s.vertical ? long * 0.9 : 0.7);
            const mesh = new THREE.Mesh(
                geometry,
                new THREE.MeshStandardMaterial({ color, roughness: 0.5, metalness: 0.35 }));
            mesh.position.set(
                s.x - half + (s.vertical ? 0.5 : long / 2),
                sunk ? -0.9 : 0.35,
                s.y - half + (s.vertical ? long / 2 : 0.5));
            if (sunk) mesh.rotation.z = 0.35;
            state.scene.add(mesh);
            state.ships.push(mesh);
        };

        for (const s of scene.friendly) place(s, 0x8fb8c9, s.sunk);
        for (const s of scene.wrecks) place(s, 0x6b3f3f, true);
    },

    freeze(frozen) { state.frozen = !!frozen; },

    dispose() {
        cancelAnimationFrame(state.raf);
        if (state.scene) disposeShips();
        if (state.renderer) state.renderer.dispose();
        state.renderer = null;
        state.scene = null;
    }
};
```

**Ce que ce fichier ne fait pas, volontairement** : aucun `raycaster`, aucun gestionnaire de clic, aucun contrôle de caméra. Le jeu est le DOM.

- [ ] **Étape 4 : charger le module**

Dans `BattleShip.App/wwwroot/index.html`, avant la fermeture de `<body>` et **après** le bloc `battleshipSettings` :

```html
<script type="module" src="js/scene.js"></script>
```

`type="module"` est requis : `scene.js` fait un `import`.

- [ ] **Étape 5 : vérifier que rien n'est cassé**

Run: `dotnet build`
Expected: **0 avertissement**.

Run: `dotnet test`
Expected: **174/174**.

Le module n'est pas encore appelé : à ce stade on vérifie seulement que le front compile et que le fichier est servi.

- [ ] **Étape 6 : commit**

```bash
dotnet format
git add BattleShip.App/wwwroot/lib/three.module.js BattleShip.App/wwwroot/js/scene.js BattleShip.App/wwwroot/index.html
git commit -m "feat: vendorise Three.js et ajoute l'objet d'interop de scène

battleshipScene suit la forme de battleshipSettings déjà présent :
un objet, des fonctions courtes, chacune tolérante à l'échec. Aucun
raycaster ni contrôle de caméra — le jeu reste le DOM.

Three.js est servi par le front, pas par un CDN : le README documente
déjà une dégradation hors-ligne, on n'en ajoute pas une seconde.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 4 : le composant `SceneCanvas`

**Files:**
- Create: `BattleShip.App/Components/SceneCanvas.razor`
- Modify: `BattleShip.App/wwwroot/css/app.css`

**Interfaces:**
- Consumes: `battleshipScene` (tâche 3), `SceneState` (tâche 1)
- Produces: `<SceneCanvas State="@sceneState" />`

- [ ] **Étape 1 : écrire le composant**

```razor
@using BattleShip.Models
@implements IAsyncDisposable
@inject IJSRuntime Js

<canvas @ref="_canvas" class="scene-canvas" aria-hidden="true"></canvas>

@code {
    [Parameter] public SceneState? State { get; set; }

    private ElementReference _canvas;
    private bool _ready;
    private SceneState? _sent;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {
                _ready = await Js.InvokeAsync<bool>("battleshipScene.init", _canvas);
                if (_ready)
                {
                    var calm = await Js.InvokeAsync<bool>("battleshipSettings.prefersReducedMotion");
                    await Js.InvokeVoidAsync("battleshipScene.freeze", calm);
                }
            }
            catch (JSException)
            {
                _ready = false;
            }
        }

        if (_ready && State is not null && !ReferenceEquals(State, _sent))
        {
            _sent = State;
            try
            {
                await Js.InvokeVoidAsync("battleshipScene.update", State);
            }
            catch (JSException)
            {
            }
        }
    }

    public async ValueTask DisposeAsync()
    {
        if (!_ready)
            return;

        try
        {
            await Js.InvokeVoidAsync("battleshipScene.dispose");
        }
        catch (JSException)
        {
        }
    }
}
```

`init` rendant `false` (pas de WebGL) laisse `_ready` à `false` : le canvas reste vide et le fond existant s'affiche. **Jamais de rectangle noir.**

- [ ] **Étape 2 : positionner le canvas**

À la fin de `BattleShip.App/wwwroot/css/app.css` :

```css
.scene-canvas {
    position: fixed;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    pointer-events: none;
    display: block;
}
```

`z-index: -1` et `pointer-events: none` : le canvas ne capte jamais un clic destiné à la grille.

- [ ] **Étape 3 : brancher dans `Play.razor`**

Ajouter le composant en tête du balisage de la page, et un champ calculé :

```razor
<SceneCanvas State="@SceneFor(game)" />
```

```csharp
    private SceneState? SceneFor(GameDto game) =>
        _events.Count == 0
            ? null
            : ScenePlan.For(game.Id, game, _events, _replayCursor,
                lastImpact: _opponentImpact?.At);
```

**Le composant reçoit la même projection que la grille, au même instant** — curseur de rejeu compris. Une scène figée sur le présent pendant que la grille remonte le temps serait la régression R18 reproduite dans un médium sans test.

- [ ] **Étape 4 : vérifier**

Run: `dotnet build`
Expected: **0 avertissement**.

Run: `dotnet test`
Expected: **174/174**.

- [ ] **Étape 5 : commit**

```bash
dotnet format
git add BattleShip.App/Components/SceneCanvas.razor BattleShip.App/wwwroot/css/app.css BattleShip.App/Pages/Play.razor
git commit -m "feat: l'arrière-plan 3D suit l'état du jeu

Le canvas est aria-hidden, sans pointeur, en z-index négatif : le jeu
reste le DOM. Il reçoit la même projection que la grille, curseur de
rejeu compris — une scène figée sur le présent pendant que la grille
remonte le temps serait la régression R18 dans un médium sans test.

Sans WebGL, init rend false et le fond existant s'affiche.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 5 : vérification au navigateur

Aucun test ne couvre le rendu. Cette tâche produit les **preuves**.

**Files:** aucun (vérification)

- [ ] **Étape 1 : relancer les serveurs sur le build courant**

```bash
pkill -f "dotnet run --project BattleShip" || true
dotnet run --project BattleShip.API &
dotnet run --project BattleShip.App &
```

Noter les ports affichés par chaque `Now listening on:`.

- [ ] **Étape 2 : énoncer les résultats attendus AVANT d'ouvrir**

Écrire ces six attendus dans le rapport **avant** d'exécuter quoi que ce soit :

1. La mer est visible derrière la grille et ondule ; la grille reste lisible par-dessus.
2. Cliquer une case tire bien — le canvas ne vole aucun clic.
3. En cours de partie, `scene.children` ne contient **aucun** objet à la position d'un navire adverse non coulé.
4. Couler un navire adverse fait apparaître une épave sombre, immergée.
5. Bouger le curseur de rejeu en arrière fait **disparaître** l'épave ; en avant, elle revient.
6. `prefers-reduced-motion` ou le réglage « calme » **fige** la mer.

- [ ] **Étape 3 : jouer les six scénarios et consigner l'observé**

Pour le point 3, depuis la console :

```js
battleshipScene && window.__scene
```

Si le graphe n'est pas exposé, l'inspecter autrement — mais **ne pas exposer une variable globale juste pour le test** : lire plutôt la charge utile envoyée à `update` via un point d'arrêt, ou instrumenter temporairement sans committer.

- [ ] **Étape 4 : vérifier la dégradation sans WebGL**

Dans Chrome : `chrome://settings` → désactiver l'accélération matérielle, ou lancer avec `--disable-gpu`.
Expected : pas de rectangle noir, le fond existant s'affiche, **aucune erreur en console**.

- [ ] **Étape 5 : vérifier l'absence de fuite de contexte**

Naviguer entre `/play` et l'accueil cinq fois, puis en console :

```js
performance.memory && performance.memory.usedJSHeapSize
```

Expected : pas de croissance monotone franche. Une fuite de contexte WebGL se voit aussi par l'avertissement Chrome « Too many active WebGL contexts ».

- [ ] **Étape 6 : consigner**

Rapport structuré selon les trois registres du `CLAUDE.md` § 6.4 : **constaté**, **supposé**, **reste à vérifier**. Ne pas écrire « ça marche » sans la manipulation et son résultat.

---

## Tâche 6 : les livrables

**Files:**
- Modify: `PROMPTS.md`, `REVUE-IA.md`, `README.md`, `docs/conformite.md`

- [ ] **Étape 1 : la revue dans `REVUE-IA.md`**

Sujet : **le secret dans un graphe de scène**. Gabarit du `CLAUDE.md` § 5.3.

- *Hypothèse* : aucun objet de la scène ne porte la position d'un navire adverse non coulé.
- *Pouvoir discriminant* : les tests 1 et 3 de `ScenePlanTests` forment une paire — chacun seul est satisfait par une implémentation fausse.
- *Ce que la relecture a coûté* : la première rédaction de la spec plaçait la fonction dans `BattleShip.App` ; un spike a montré `CS0433`. Consigner la sortie.
- *Limites* : le rendu lui-même n'est couvert par aucun test ; la dégradation sans WebGL et l'absence de fuite de contexte sont vérifiées à la main, une fois.

- [ ] **Étape 2 : `PROMPTS.md`**

Une entrée au format condensé des entrées existantes. Y consigner le spike `CS0433` (attendu puis observé) et la décision qui en découle.

- [ ] **Étape 3 : `README.md`**

- Fonctionnalités livrées : l'arrière-plan 3D réactif.
- Limites connues : **pendant une partie en cours, l'arrière-plan ne montre que la mer, la flotte du joueur et les épaves** — le secret l'interdit autrement ; la cascade de naufrages est une scène de fin de partie ou de rediffusion. Ajouter aussi : sans WebGL, l'arrière-plan est absent (dégradation voulue) ; le rendu n'est couvert par aucun test automatique.
- Prérequis : aucun nouveau — Three.js est vendorisé, rien à installer.

- [ ] **Étape 4 : `docs/conformite.md`**

Une ligne par exigence nouvellement couverte, **avec une commande par exigence**. Exécuter chaque commande et écrire le nombre réellement observé — la revue finale du chantier précédent avait trouvé cinq constats périmés faute de ça.

- [ ] **Étape 5 : vérification finale**

```bash
dotnet build
dotnet test
dotnet format --verify-no-changes
grep -rnE "//|/\*" --include=*.cs --include=*.razor BattleShip.Models BattleShip.API BattleShip.App | grep -v "https://"
```

La dernière commande doit ne rien rendre. `scene.js` est en JavaScript et n'est pas couvert par cette règle, comme `index.html`.

- [ ] **Étape 6 : commit**

```bash
git add PROMPTS.md REVUE-IA.md README.md docs/conformite.md
git commit -m "docs: consigne le rendu 3D et la revue sur le secret dans la scène

La revue porte sur une fuite qu'aucun test d'intégration n'aurait vue :
un graphe de scène se lit depuis la console. Les tests 1 et 3 de
ScenePlanTests l'encadrent.

Limite consignée : pendant une partie, le secret réduit l'arrière-plan
à la mer, la flotte du joueur et les épaves.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Auto-revue du plan

**1. Couverture de la spec**

| section de la spec | tâche |
|---|---|
| § 1 canvas décoratif, jeu au DOM | 0 (ADR), 4 |
| § 2 le secret, filtrage à la source | 1 |
| § 2 conséquence sur l'ambition | 6 (README) |
| § 3 contrat d'interop | 3 |
| § 3 la scène suit le curseur de rejeu | 1 (test 4), 4 |
| § 4 dépendance vendorisée | 3 |
| § 4 dégradation sans WebGL | 4, 5 |
| § 4 mouvement réduit | 4 |
| § 4 cycle de vie | 4, 5 |
| § 5 les quatre tests | 1 |
| § 5 où vit la fonction (spike) | 0, 1 |
| § 5 non vérifiable automatiquement | 5 |
| § 6 hors périmètre | 0 (ADR) |

Aucune section sans tâche.

**2. Placeholders** — aucun `TBD`, aucun « gérer les erreurs », aucun « comme la tâche N ». Le code est écrit en entier.

**3. Cohérence des types** — `SceneShip(Name, X, Y, Size, Vertical, Sunk)` défini en tâche 1, consommé tel quel en tâche 2 (`Footprint`), en tâche 3 (`s.size`, `s.vertical`, `s.x`, `s.y`, `s.sunk` — camelCase côté JS, la sérialisation Blazor l'assure) et en tâche 4. `ScenePlan.For(Guid, GameDto, IReadOnlyList<GameEvent>, int?, Coordinate?)` défini en tâche 1, appelé avec la même signature en tâches 2 et 4.

**Trois points à relire avec attention pendant l'exécution :**

- **Tâche 1** — `Prefix` doit compter les `ShotFired` *tous joueurs confondus*, comme `ReplayedGame`. Un décalage afficherait l'épave au mauvais coup.
- **Tâche 1** — `GameFold.Fold` lève sur un journal vide ; la garde `upTo.Count == 0` est obligatoire.
- **Tâche 3** — la casse des propriétés à la frontière C#/JS. Blazor sérialise en camelCase par défaut ; si `scene.friendly` arrive `undefined`, c'est là qu'il faut regarder en premier.
