# Plan d'implémentation — Le socle 3D jouable

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Le plateau se joue dans une scène 3D — survol et tir à la souris — sans perdre une seule des garanties d'accessibilité déjà livrées.

**Architecture:** La scène 3D est **ce qui se voit** ; les 100 boutons de grille restent **ce qui s'annonce**, visuellement effacés mais focalisables. Aucun alignement géométrique entre les deux : le focus DOM éclaire la case 3D par son index. La projection `ScenePlan` reste la source unique et gagne les états de case dont la 3D a besoin.

**Tech Stack:** .NET 10, Blazor WebAssembly, xUnit, Three.js r169 (déjà vendorisé), WebGL.

**Spec:** `docs/superpowers/specs/2026-09-22-jeu-3d-design.md`

## Global Constraints

- **Langue** : code en **anglais** ; documentation humaine et **messages de commit** en **français**.
- **Aucun commentaire dans le C# ni le Razor.** `wwwroot/index.html` et les `.js` suivent le style local du fichier, sobrement.
- **Nommage** : `PascalCase` types/méthodes/propriétés ; `_camelCase` champs privés ; suffixe `Async` sur tout `Task`.
- **Noms de tests** : anglais descriptif, underscores.
- **Nullable activé** ; `sealed` par défaut ; aucun `.Result` ni `.Wait()`.
- **`dotnet build` à ZÉRO avertissement** ; `dotnet format` avant chaque commit.
- **Le secret** : aucune position d'un navire adverse non coulé ne doit atteindre le front — scène 3D comprise. Le graphe de scène est lisible depuis la console.
- **L'accessibilité ne régresse pas** : les boutons de grille gardent `aria-label`, `tabindex` roving, flèches, et la région `aria-live`.
- **Le serveur ne change pas.** Le tir passe par le même `FireAsync`.
- Message de commit terminé par : `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

### Ligne de base mesurée le 2026-09-22

```
dotnet build   → 0 avertissement
dotnet test    → 191/191
```

### Piège d'environnement, à connaître avant toute vérification à l'écran

Blazor sert les assets de `wwwroot/` via une **import map à empreintes** :

```
"./js/scene.js"          →  "./js/scene.5vtkj536jo.js"
"./lib/three.module.js"  →  "./lib/three.module.ejpzjpx7xu.js"
```

Éditer un fichier de `wwwroot/` pendant que `dotnet run` tourne rend ces empreintes périmées : le module n'est jamais évalué, **sans une seule erreur en console**. Le jeu fonctionne, il manque juste toute la 3D.

**Donc : toute vérification à l'écran se fait sur un serveur fraîchement relancé, et commence par confirmer que le module est chargé.**

---

## Structure des fichiers

| fichier | responsabilité |
|---|---|
| `BattleShip.Models/Scene/SceneState.cs` | **modifié** — `SceneCell` + deux listes d'états de case |
| `BattleShip.Models/Scene/ScenePlan.cs` | **modifié** — peuple ces listes depuis le journal |
| `BattleShip.Tests/Domain/ScenePlanTests.cs` | **modifié** — le test de non-fuite parcourt le JSON |
| `BattleShip.App/wwwroot/js/game3d/scene.js` | **créé** — mer, plateaux, grille projetée |
| `BattleShip.App/wwwroot/js/game3d/input.js` | **créé** — raycasting, survol, clic |
| `BattleShip.App/wwwroot/js/game3d/camera.js` | **créé** — cadrage, plongée, responsive |
| `BattleShip.App/wwwroot/js/game3d/index.js` | **créé** — l'objet d'interop `battleshipGame3d` |
| `BattleShip.App/wwwroot/js/scene.js` | **supprimé** — le décor est remplacé |
| `BattleShip.App/Components/GameCanvas.razor` | **créé** — le composant, `IAsyncDisposable` |
| `BattleShip.App/Components/FiringGrid.razor` | **modifié** — mode « couche d'annonce » |
| `BattleShip.App/wwwroot/css/app.css` | **modifié** — grille effacée, canvas de jeu |
| `BattleShip.App/Pages/Play.razor` | **modifié** — câblage |
| `BattleShip.App/wwwroot/index.html` | **modifié** — charge le nouveau module |
| `docs/adr/0013-jeu-3d.md` | **créé** — **remplace l'ADR 0012**, avant tout code |

---

## Tâche 0 : l'ADR 0013, qui remplace le 0012

**Files:** Create `docs/adr/0013-jeu-3d.md` · Modify `docs/adr/0012-rendu-3d.md`

**Interfaces:** Consumes la spec · Produces rien de codé

- [ ] **Étape 1 : lire l'ADR remplacé et le gabarit**

Run: `cat docs/adr/0012-rendu-3d.md docs/adr/0009-trois-skins.md`

- [ ] **Étape 2 : écrire l'ADR 0013**

Gabarit à six sections du `CLAUDE.md` § 5.2. Date 2026-09-22, statut **Accepté**. Contenu obligatoire :

1. **Ce qui est renversé.** L'ADR 0012 décidait « Un arrière-plan 3D décoratif, pas un jeu en 3D ». Le binôme a tranché l'inverse. Cite la décision d'origine, ne la paraphrase pas.
2. **Pourquoi son objection ne s'applique plus.** Elle était fondée : un `<canvas>` n'a pas de DOM, donc navigation clavier, `aria-live`, glyphes et contrastes auraient dû être réimplémentés. La réponse est de **ne pas les réimplémenter** — les boutons restent, visuellement effacés, focalisables, et le focus éclaire la case 3D par son index.
3. **Le fait qui rend ça possible** : un lecteur d'écran **énumère et annonce**, il ne clique pas. Il se moque de la position à l'écran. D'où : aucun alignement géométrique, donc caméra libre.
4. **Les deux approches écartées** : reprojeter la grille DOM à chaque image (100 éléments repositionnés par frame, désalignement au moindre mouvement, couche d'accessibilité mouvante) ; caméra orthographique figée (annule l'intérêt du passage en 3D).
5. **Le secret dans un graphe de scène**, inchangé depuis l'ADR 0012 : filtrage à la source, jamais à l'affichage. Et le durcissement : le test de non-fuite **parcourt l'objet sérialisé** au lieu d'énumérer les champs, parce que la revue précédente avait nommé ce trou.
6. **Conséquences** : `scene.js` est supprimé ; `ScenePlan` et ses 22 tests survivent inchangés ; le serveur ne bouge pas.
7. **Vérification et réexamen** : renvoyer aux 4 tests du § 5 de la spec et aux 4 scénarios écran. Condition de réexamen : si le calque d'annonce devait un jour être aligné géométriquement, l'arbitrage serait à refaire.

- [ ] **Étape 3 : marquer l'ADR 0012 comme remplacé**

Dans `docs/adr/0012-rendu-3d.md`, ligne de statut : ajouter **« Remplacé par l'ADR 0013 — 2026-09-22 »**. Ne supprime rien d'autre : le `CLAUDE.md` § 5.2 exige l'historique conservé.

- [ ] **Étape 4 : commit**

```bash
git add docs/adr/0013-jeu-3d.md docs/adr/0012-rendu-3d.md
git commit -m "docs: ADR 0013 — le jeu passe en 3D, l'ADR 0012 est remplacé

Sa décision centrale s'inverse. Son objection sur l'accessibilité était
fondée et reste vraie : elle est contournée en gardant les boutons comme
couche d'annonce plutôt qu'en les réimplémentant.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 1 : `SceneState` porte les états de case, et le test de non-fuite devient générique

Le cœur du chantier côté C#. Tout le reste est du dessin.

**Files:**
- Modify: `BattleShip.Models/Scene/SceneState.cs`
- Modify: `BattleShip.Models/Scene/ScenePlan.cs`
- Test: `BattleShip.Tests/Domain/ScenePlanTests.cs`

**Interfaces:**
- Consumes: `GameDto`, `GameEvent`, `ShotFired`, `Coordinate`, `Player`, `ShotResult` (existants)
- Produces:
  - `public sealed record SceneCell(int X, int Y, string State)` — `State` vaut `"miss"`, `"hit"` ou `"sunk"`
  - `SceneState` gagne `IReadOnlyList<SceneCell> OpponentCells` et `IReadOnlyList<SceneCell> OwnCells`, **insérés après `Wrecks`**

- [ ] **Étape 1 : rendre générique le test de non-fuite existant**

**Ne crée pas de nouveau test et n'en supprime aucun.** Le fichier en porte déjà deux, et chacun tient quelque chose que l'autre n'a pas :

- `During_a_game_no_unsunk_opposing_cell_reaches_the_scene` (ligne 84) porte en plus `Assert.Equal(humanCells, friendlyCells)` — l'égalité ensembliste de la flotte amie, qui est la correction R1 du chantier précédent. **Ce test ne change pas.**
- `At_every_cursor_on_either_journal_no_unsunk_opposing_cell_reaches_the_scene` (ligne 117) calcule `TrueSecretAt(id, events, cursor)` — le secret **à ce curseur**, et joue `shots: 500`. C'est strictement plus fort qu'un secret calculé une fois sur l'état final, qui manquerait une case secrète au curseur 3 mais coulée à la fin. **C'est celui-ci qu'on généralise.**

Ajouter le parcours générique, en helpers privés :

```csharp
    private static IEnumerable<Coordinate> AllCoordinatesIn(SceneState scene)
    {
        var json = System.Text.Json.JsonSerializer.Serialize(scene);
        using var document = System.Text.Json.JsonDocument.Parse(json);
        return Walk(document.RootElement).ToList();
    }

    private static IEnumerable<Coordinate> Walk(System.Text.Json.JsonElement element)
    {
        if (element.ValueKind == System.Text.Json.JsonValueKind.Object)
        {
            if (element.TryGetProperty("X", out var x) && element.TryGetProperty("Y", out var y)
                && x.ValueKind == System.Text.Json.JsonValueKind.Number
                && y.ValueKind == System.Text.Json.JsonValueKind.Number)
            {
                yield return new Coordinate(x.GetInt32(), y.GetInt32());
            }

            foreach (var property in element.EnumerateObject())
                foreach (var found in Walk(property.Value))
                    yield return found;
        }
        else if (element.ValueKind == System.Text.Json.JsonValueKind.Array)
        {
            foreach (var item in element.EnumerateArray())
                foreach (var found in Walk(item))
                    yield return found;
        }
    }

    private static HashSet<Coordinate> Exposed(SceneState scene) =>
        AllCoordinatesIn(scene)
            .Concat(scene.Wrecks.SelectMany(Footprint))
            .Concat(scene.Friendly.SelectMany(Footprint))
            .ToHashSet();
```

puis, **dans `At_every_cursor_on_either_journal_no_unsunk_opposing_cell_reaches_the_scene` uniquement**, remplacer les deux assertions :

```csharp
            Assert.Empty(full.Wrecks.SelectMany(Footprint).ToHashSet().Intersect(secret));
            Assert.Empty(onCensored.Wrecks.SelectMany(Footprint).ToHashSet().Intersect(secret));
```

par :

```csharp
            Assert.Empty(Exposed(full).Intersect(secret));
            Assert.Empty(Exposed(onCensored).Intersect(secret));
```

Le reste du test — `Played(seed, shots: 500)`, la boucle sur les curseurs, `TrueSecretAt` — **ne bouge pas**.

**Pourquoi l'union et pas le parcours seul.** `AllCoordinatesIn` lit la paire `X`/`Y` d'un `SceneShip`, c'est-à-dire son **ancre**, pas son emprise : un navire de taille 4 en (3,4) occupe aussi (3,5), (3,6), (3,7), que le parcours ne verrait pas. `Footprint` déplie cette emprise. Le parcours apporte la couverture des champs **futurs**, `Footprint` garde l'exactitude sur les navires. Les deux, pas l'un.

**Limite qui subsiste, à écrire telle quelle dans la revue** : un champ ajouté plus tard qui porterait lui aussi une *forme* (ancre + taille + orientation) ne serait attrapé que par son ancre. Le parcours réduit le trou, il ne le ferme pas.

- [ ] **Étape 2 : exécuter, et constater que ça passe**

Run: `dotnet test --filter "FullyQualifiedName~ScenePlanTests"`
Expected: **PASS**, 22/22.

C'est attendu, et ça ne prouve encore rien : `SceneState` n'a pas les nouveaux champs, donc le parcours ne trouve rien de plus que `Footprint`. Le test ne commence à travailler qu'à l'étape 5, une fois les champs ajoutés. Note le chiffre ici pour pouvoir comparer.

- [ ] **Étape 3 : étendre `SceneState`**

```csharp
namespace BattleShip.Models;

public sealed record SceneShip(string Name, int X, int Y, int Size, bool Vertical, bool Sunk);

public sealed record SceneCell(int X, int Y, string State);

public sealed record SceneState(
    int GridSize,
    IReadOnlyList<SceneShip> Friendly,
    IReadOnlyList<SceneShip> Wrecks,
    IReadOnlyList<SceneCell> OpponentCells,
    IReadOnlyList<SceneCell> OwnCells,
    Coordinate? LastImpact,
    bool Revealed);
```

- [ ] **Étape 4 : peupler les deux listes dans `ScenePlan.For`**

Dans `ScenePlan.For`, après le calcul de `wrecks` et avant le `return`, ajouter :

```csharp
        var shots = cursor is { } upTo
            ? Prefix(events, upTo).OfType<ShotFired>().ToList()
            : events.OfType<ShotFired>().ToList();

        var opponentCells = shots
            .Where(s => s.By == Player.Human)
            .Select(s => new SceneCell(s.At.X, s.At.Y, StateOf(s.Result)))
            .ToList();

        var ownCells = shots
            .Where(s => s.By == Player.Opponent)
            .Select(s => new SceneCell(s.At.X, s.At.Y, StateOf(s.Result)))
            .ToList();
```

et remplacer le `return` par :

```csharp
        return new SceneState(
            game.Own.GridSize, friendly, wrecks, opponentCells, ownCells, lastImpact, revealed);
```

Ajouter la conversion, privée :

```csharp
    private static string StateOf(ShotResult result) => result switch
    {
        ShotResult.Miss => "miss",
        ShotResult.Hit => "hit",
        ShotResult.Sunk => "sunk",
        _ => throw new ArgumentOutOfRangeException(nameof(result), result, "Unknown shot result.")
    };
```

**Le point délicat** : les deux listes se dérivent **du journal**, pas du `GameDto`. C'est ce qui les fait suivre le curseur de rejeu gratuitement, avec exactement le comptage que `Prefix` applique déjà aux épaves. Ne les dérive pas de `game.Opponent.Shots` : cette liste est l'état courant et ignorerait le curseur.

- [ ] **Étape 5 : ajouter le test de fidélité des états de case**

```csharp
    [Fact]
    public void The_cell_states_mirror_the_shots_of_the_journal()
    {
        var (id, dto, events, _) = Played(seed: 41, shots: 12);

        var scene = ScenePlan.For(id, dto, events, cursor: null, lastImpact: null);

        var humanShots = events.OfType<ShotFired>().Where(s => s.By == Player.Human).ToList();
        var opponentShots = events.OfType<ShotFired>().Where(s => s.By == Player.Opponent).ToList();

        Assert.Equal(humanShots.Count, scene.OpponentCells.Count);
        Assert.Equal(opponentShots.Count, scene.OwnCells.Count);

        Assert.Equal(
            humanShots.Select(s => (s.At.X, s.At.Y)),
            scene.OpponentCells.Select(c => (c.X, c.Y)));

        Assert.All(scene.OpponentCells, c => Assert.Contains(c.State, new[] { "miss", "hit", "sunk" }));
    }

    [Fact]
    public void At_a_cursor_the_cell_states_stop_at_that_instant()
    {
        var (id, dto, events, _) = Played(seed: 41, shots: 12);

        var full = ScenePlan.For(id, dto, events, cursor: null, lastImpact: null);
        var early = ScenePlan.For(id, dto, events, cursor: 3, lastImpact: null);

        Assert.True(early.OpponentCells.Count + early.OwnCells.Count == 3);
        Assert.True(full.OpponentCells.Count + full.OwnCells.Count > 3);
    }
```

Le second test est celui qui attrape une dérivation depuis `game.Opponent.Shots` : elle rendrait l'état courant quel que soit le curseur, donc les deux comptes seraient égaux.

- [ ] **Étape 6 : exécuter**

Run: `dotnet test --filter "FullyQualifiedName~ScenePlanTests"`
Expected: **PASS**.

Run: `dotnet test`
Expected: **193/193** — 191 de la ligne de base, plus les deux `[Fact]` de l'étape 5. Aucun test n'est supprimé ni remplacé. Si le chiffre observé diffère, c'est lui qu'on écrit, et on explique l'écart.

- [ ] **Étape 7 : commit**

```bash
dotnet format
git add BattleShip.Models/Scene/ BattleShip.Tests/Domain/ScenePlanTests.cs
git commit -m "feat: SceneState porte les états de case, et la non-fuite devient générique

Le test de non-fuite cesse d'énumérer les champs : il sérialise
SceneState et remonte toute paire X/Y à n'importe quelle profondeur.
C'est exactement le trou que la revue finale du chantier précédent avait
nommé — un champ ajouté plus tard est désormais couvert sans que
personne y pense.

Les états de case se dérivent du journal, pas du GameDto : ils suivent
donc le curseur de rejeu avec le même comptage que les épaves.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 2 : la scène 3D — mer, plateaux, grille projetée

Aucune interaction à ce stade. On construit ce qui se voit.

**Files:**
- Create: `BattleShip.App/wwwroot/js/game3d/scene.js`
- Create: `BattleShip.App/wwwroot/js/game3d/camera.js`

**Interfaces:**
- Consumes: `../../lib/three.module.js` (r169, déjà vendorisé)
- Produces:
  - `scene.js` : `createScene(renderer)` → `{ scene, boards, setState(state), dispose() }` où `boards` est `{ opponent, own }`, chacun un `THREE.Group` portant `userData.origin` (coin de la grille) et `userData.cellSize`
  - `camera.js` : `createCamera(aspect)` → `{ camera, frame(width), lookAtCell(x, y, gridSize), dispose() }`

- [ ] **Étape 1 : écrire `camera.js`**

```js
import * as THREE from '../../lib/three.module.js';

export function createCamera(aspect) {
    const camera = new THREE.PerspectiveCamera(45, aspect, 0.1, 400);
    let target = new THREE.Vector3(0, 0, 0);

    function frame(width) {
        const wide = width >= 900;
        camera.position.set(0, wide ? 30 : 38, wide ? 34 : 44);
        camera.lookAt(target);
    }

    function lookAtCell(x, y, gridSize) {
        const half = gridSize / 2;
        target = new THREE.Vector3(x - half + 0.5, 0, y - half + 0.5);
        camera.lookAt(target);
    }

    frame(typeof window !== 'undefined' ? window.innerWidth : 1200);
    return { camera, frame, lookAtCell, dispose() { target = null; } };
}
```

- [ ] **Étape 2 : écrire `scene.js`**

```js
import * as THREE from '../../lib/three.module.js';

const CELL = 1;

function makeBoard(gridSize, offsetX) {
    const group = new THREE.Group();
    const span = gridSize * CELL;

    const plate = new THREE.Mesh(
        new THREE.BoxGeometry(span, 0.3, span),
        new THREE.MeshStandardMaterial({ color: 0x0f2d3d, roughness: 0.8 }));
    plate.position.set(offsetX, 0.15, 0);
    group.add(plate);

    const lines = [];
    for (let i = 0; i <= gridSize; i++) {
        const p = i * CELL - span / 2;
        lines.push(offsetX + p, 0.31, -span / 2, offsetX + p, 0.31, span / 2);
        lines.push(offsetX - span / 2, 0.31, p, offsetX + span / 2, 0.31, p);
    }
    const geometry = new THREE.BufferGeometry();
    geometry.setAttribute('position', new THREE.Float32BufferAttribute(lines, 3));
    group.add(new THREE.LineSegments(
        geometry, new THREE.LineBasicMaterial({ color: 0x6f9db2, transparent: true, opacity: 0.55 })));

    group.userData.originX = offsetX - span / 2;
    group.userData.originZ = -span / 2;
    group.userData.cellSize = CELL;
    group.userData.gridSize = gridSize;
    return group;
}

export function createScene(renderer) {
    const scene = new THREE.Scene();
    scene.add(new THREE.HemisphereLight(0x9fc6d8, 0x08161f, 1.2));
    const sun = new THREE.DirectionalLight(0xffffff, 0.9);
    sun.position.set(14, 30, 18);
    scene.add(sun);

    const seaGeometry = new THREE.PlaneGeometry(300, 300, 80, 80);
    seaGeometry.rotateX(-Math.PI / 2);
    const sea = new THREE.Mesh(
        seaGeometry,
        new THREE.MeshStandardMaterial({ color: 0x14536e, roughness: 0.5, metalness: 0.2 }));
    sea.position.y = -0.4;
    scene.add(sea);

    let boards = null;
    let marks = [];
    let ships = [];

    function clear(list) {
        for (const mesh of list) {
            scene.remove(mesh);
            mesh.geometry.dispose();
            mesh.material.dispose();
        }
        return [];
    }

    function cellCentre(board, x, y) {
        return {
            x: board.userData.originX + (x + 0.5) * board.userData.cellSize,
            z: board.userData.originZ + (y + 0.5) * board.userData.cellSize
        };
    }

    const MARK_COLOURS = { miss: 0x7f97a4, hit: 0xd08a3a, sunk: 0x8c3b3b };

    function setState(state) {
        if (!state) return;

        if (!boards) {
            const gap = state.gridSize * CELL * 0.9;
            boards = {
                own: makeBoard(state.gridSize, -gap),
                opponent: makeBoard(state.gridSize, gap)
            };
            scene.add(boards.own);
            scene.add(boards.opponent);
        }

        marks = clear(marks);
        ships = clear(ships);

        const addMark = (board, cell) => {
            const centre = cellCentre(board, cell.x, cell.y);
            const mesh = new THREE.Mesh(
                new THREE.CylinderGeometry(0.32, 0.32, 0.12, 16),
                new THREE.MeshStandardMaterial({ color: MARK_COLOURS[cell.state] || 0x7f97a4 }));
            mesh.position.set(centre.x, 0.38, centre.z);
            scene.add(mesh);
            marks.push(mesh);
        };

        for (const cell of state.opponentCells) addMark(boards.opponent, cell);
        for (const cell of state.ownCells) addMark(boards.own, cell);

        const addShip = (board, s, colour, sunk) => {
            const long = s.size * CELL * 0.9;
            const mesh = new THREE.Mesh(
                new THREE.BoxGeometry(s.vertical ? 0.7 : long, 0.5, s.vertical ? long : 0.7),
                new THREE.MeshStandardMaterial({ color: colour, roughness: 0.45, metalness: 0.35 }));
            const centre = cellCentre(
                board,
                s.x + (s.vertical ? 0 : (s.size - 1) / 2),
                s.y + (s.vertical ? (s.size - 1) / 2 : 0));
            mesh.position.set(centre.x, sunk ? -0.1 : 0.55, centre.z);
            if (sunk) mesh.rotation.z = 0.3;
            scene.add(mesh);
            ships.push(mesh);
        };

        for (const s of state.friendly) addShip(boards.own, s, 0x8fb8c9, s.sunk);
        for (const s of state.wrecks) addShip(boards.opponent, s, 0x6b3f3f, true);
    }

    function dispose() {
        marks = clear(marks);
        ships = clear(ships);
        seaGeometry.dispose();
        sea.material.dispose();
        boards = null;
    }

    return { scene, get boards() { return boards; }, setState, dispose };
}
```

**Deux pièges signalés d'avance.** `setState` doit être **idempotent** : il libère `marks` et `ships` avant de les reconstruire, sinon deux appels empilent des objets. Et `boards` n'est construit qu'une fois : le reconstruire à chaque état ferait perdre la géométrie de grille et coûterait cher.

- [ ] **Étape 3 : vérifier que rien n'est cassé**

Run: `dotnet build` — Expected: **0 avertissement**.
Run: `dotnet test` — Expected: le total de la tâche 1, inchangé.

Les modules ne sont encore appelés par personne.

- [ ] **Étape 4 : commit**

```bash
dotnet format
git add BattleShip.App/wwwroot/js/game3d/
git commit -m "feat: la scène 3D — mer, deux plateaux, grille projetée

setState est idempotent : il libère marques et coques avant de les
reconstruire. Les plateaux ne sont construits qu'une fois, leur
géométrie de grille étant coûteuse et invariante.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 3 : le survol et le tir par raycasting

**Files:** Create `BattleShip.App/wwwroot/js/game3d/input.js`

**Interfaces:**
- Consumes: `createScene` (tâche 2) — `boards.opponent` avec `userData.originX/originZ/cellSize/gridSize`
- Produces: `createInput(renderer, camera, boards, onPick)` → `{ setHover(x, y), dispose() }` ; `onPick(x, y)` est appelé au clic sur une case du plateau adverse

- [ ] **Étape 1 : écrire `input.js`**

```js
import * as THREE from '../../lib/three.module.js';

export function createInput(renderer, camera, getBoards, onPick) {
    const raycaster = new THREE.Raycaster();
    const pointer = new THREE.Vector2();
    let highlight = null;
    let scene = null;

    function cellUnder(event) {
        const boards = getBoards();
        if (!boards) return null;

        const rect = renderer.domElement.getBoundingClientRect();
        pointer.x = ((event.clientX - rect.left) / rect.width) * 2 - 1;
        pointer.y = -((event.clientY - rect.top) / rect.height) * 2 + 1;
        raycaster.setFromCamera(pointer, camera);

        const hits = raycaster.intersectObject(boards.opponent, true);
        if (hits.length === 0) return null;

        const board = boards.opponent;
        const point = hits[0].point;
        const x = Math.floor((point.x - board.userData.originX) / board.userData.cellSize);
        const y = Math.floor((point.z - board.userData.originZ) / board.userData.cellSize);
        const n = board.userData.gridSize;
        return (x >= 0 && x < n && y >= 0 && y < n) ? { x, y } : null;
    }

    function setHover(x, y) {
        const boards = getBoards();
        if (!boards) return;
        if (!scene) scene = boards.opponent.parent;

        if (highlight) { scene.remove(highlight); highlight.geometry.dispose(); highlight.material.dispose(); highlight = null; }
        if (x === null || x === undefined) return;

        const board = boards.opponent;
        const size = board.userData.cellSize;
        highlight = new THREE.Mesh(
            new THREE.BoxGeometry(size * 0.94, 0.06, size * 0.94),
            new THREE.MeshBasicMaterial({ color: 0xffd764, transparent: true, opacity: 0.45 }));
        highlight.position.set(
            board.userData.originX + (x + 0.5) * size, 0.34,
            board.userData.originZ + (y + 0.5) * size);
        scene.add(highlight);
    }

    function onMove(event) { const c = cellUnder(event); setHover(c ? c.x : null, c ? c.y : null); }
    function onClick(event) { const c = cellUnder(event); if (c) onPick(c.x, c.y); }
    function onLeave() { setHover(null); }

    renderer.domElement.addEventListener('pointermove', onMove);
    renderer.domElement.addEventListener('click', onClick);
    renderer.domElement.addEventListener('pointerleave', onLeave);

    return {
        setHover,
        dispose() {
            renderer.domElement.removeEventListener('pointermove', onMove);
            renderer.domElement.removeEventListener('click', onClick);
            renderer.domElement.removeEventListener('pointerleave', onLeave);
            setHover(null);
        }
    };
}
```

**Le piège à ne pas manquer** : les trois écouteurs sont posés sur `renderer.domElement` et **doivent être retirés** par `dispose`. Un chantier précédent a livré un écouteur `resize` jamais retiré, qui fuyait **et plantait** après `dispose` en appelant une méthode sur un renderer mis à `null`. Ne le refais pas.

`setHover` est exporté parce que la tâche 4 l'appelle depuis le focus clavier, pas seulement depuis la souris.

- [ ] **Étape 2 : vérifier**

Run: `dotnet build` — Expected: **0 avertissement**. Run: `dotnet test` — inchangé.

- [ ] **Étape 3 : commit**

```bash
dotnet format
git add BattleShip.App/wwwroot/js/game3d/input.js
git commit -m "feat: survol et tir par raycasting sur le plateau adverse

Les trois écouteurs sont retirés par dispose : un chantier précédent a
livré un écouteur resize jamais retiré qui fuyait et plantait après
dispose. setHover est exporté pour que le focus clavier puisse éclairer
la même case que la souris.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 4 : l'objet d'interop et le composant

**Files:**
- Create: `BattleShip.App/wwwroot/js/game3d/index.js`
- Create: `BattleShip.App/Components/GameCanvas.razor`
- Modify: `BattleShip.App/wwwroot/index.html`

**Interfaces:**
- Consumes: `createScene`, `createCamera`, `createInput`
- Produces: l'objet global `battleshipGame3d` avec `init(canvas, dotnetRef) -> bool`, `update(state)`, `hover(x, y)`, `freeze(bool)`, `dispose()` ; le composant `<GameCanvas State="@_scene" OnPick="..." />`

- [ ] **Étape 1 : écrire `index.js`**

```js
import * as THREE from '../../lib/three.module.js';
import { createScene } from './scene.js';
import { createCamera } from './camera.js';
import { createInput } from './input.js';

const state = { renderer: null, world: null, cam: null, input: null, raf: 0, frozen: false, resize: null, ref: null };

window.battleshipGame3d = {
    init(canvas, dotnetRef) {
        try {
            state.renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
        } catch (e) {
            return false;
        }

        state.ref = dotnetRef;
        state.renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
        state.world = createScene(state.renderer);
        state.cam = createCamera(1);
        state.input = createInput(
            state.renderer, state.cam.camera, () => state.world.boards,
            (x, y) => { if (state.ref) state.ref.invokeMethodAsync('PickAsync', x, y); });

        const resize = () => {
            const w = canvas.clientWidth || 1, h = canvas.clientHeight || 1;
            state.renderer.setSize(w, h, false);
            state.cam.camera.aspect = w / h;
            state.cam.camera.updateProjectionMatrix();
            state.cam.frame(w);
        };
        state.resize = resize;
        resize();
        window.addEventListener('resize', resize);

        const tick = () => {
            state.raf = requestAnimationFrame(tick);
            state.renderer.render(state.world.scene, state.cam.camera);
        };
        state.raf = requestAnimationFrame(tick);
        return true;
    },

    update(scene) { if (state.world) state.world.setState(scene); },

    hover(x, y) { if (state.input) state.input.setHover(x, y); },

    freeze(frozen) { state.frozen = !!frozen; },

    dispose() {
        cancelAnimationFrame(state.raf);
        if (state.resize) { window.removeEventListener('resize', state.resize); state.resize = null; }
        if (state.input) { state.input.dispose(); state.input = null; }
        if (state.world) { state.world.dispose(); state.world = null; }
        if (state.cam) { state.cam.dispose(); state.cam = null; }
        if (state.renderer) { state.renderer.dispose(); state.renderer = null; }
        state.ref = null;
        state.frozen = false;
    }
};
```

**`state.frozen` est remis à `false` par `dispose`** : un chantier précédent a livré l'oubli, qui faisait redémarrer la scène **gelée en silence** après un cycle.

- [ ] **Étape 2 : écrire `GameCanvas.razor`**

```razor
@using BattleShip.Models
@implements IAsyncDisposable
@inject IJSRuntime Js
@inject AppearanceState Appearance

<canvas @ref="_canvas" class="game-canvas" aria-hidden="true"></canvas>

@code {
    [Parameter] public SceneState? State { get; set; }

    [Parameter] public EventCallback<Coordinate> OnPick { get; set; }

    [Parameter] public Coordinate? Focused { get; set; }

    private ElementReference _canvas;
    private DotNetObjectReference<GameCanvas>? _self;
    private bool _ready;
    private bool _disposed;
    private SceneState? _sent;
    private Coordinate? _sentFocus;
    private bool? _sentCalm;
    private bool _osPrefersStill;

    private bool Calm => _osPrefersStill || Appearance.Motion == "calm";

    protected override void OnInitialized() => Appearance.OnChange += OnAppearanceChanged;

    private void OnAppearanceChanged() => InvokeAsync(StateHasChanged);

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {
                _self = DotNetObjectReference.Create(this);
                _ready = await Js.InvokeAsync<bool>("battleshipGame3d.init", _canvas, _self);
                if (_ready)
                    _osPrefersStill = await Js.InvokeAsync<bool>("battleshipSettings.prefersReducedMotion");
            }
            catch (JSException)
            {
                _ready = false;
            }
        }

        if (!_ready || _disposed)
            return;

        try
        {
            if (_sentCalm != Calm)
            {
                _sentCalm = Calm;
                await Js.InvokeVoidAsync("battleshipGame3d.freeze", Calm);
            }

            if (!ReferenceEquals(State, _sent))
            {
                _sent = State;
                await Js.InvokeVoidAsync("battleshipGame3d.update", State);
            }

            if (_sentFocus != Focused)
            {
                _sentFocus = Focused;
                await Js.InvokeVoidAsync(
                    "battleshipGame3d.hover", Focused?.X, Focused?.Y);
            }
        }
        catch (JSException)
        {
        }
    }

    [JSInvokable]
    public async Task PickAsync(int x, int y) => await OnPick.InvokeAsync(new Coordinate(x, y));

    public async ValueTask DisposeAsync()
    {
        _disposed = true;
        Appearance.OnChange -= OnAppearanceChanged;

        if (!_ready)
        {
            _self?.Dispose();
            return;
        }

        try
        {
            await Js.InvokeVoidAsync("battleshipGame3d.dispose");
        }
        catch (JSException)
        {
        }

        _self?.Dispose();
    }
}
```

**`_disposed` est posé AVANT le désabonnement** et testé après chaque `await` — même motif qu'un chantier précédent, où un `StateHasChanged` partait sur un composant démonté.

- [ ] **Étape 3 : charger le module**

Dans `BattleShip.App/wwwroot/index.html`, remplacer la ligne `<script type="module" src="js/scene.js"></script>` par :

```html
<script type="module" src="js/game3d/index.js"></script>
```

- [ ] **Étape 4 : vérifier**

Run: `dotnet build` — Expected: **0 avertissement**. Run: `dotnet test` — inchangé.

- [ ] **Étape 5 : commit**

```bash
dotnet format
git add BattleShip.App/wwwroot/js/game3d/index.js BattleShip.App/Components/GameCanvas.razor BattleShip.App/wwwroot/index.html
git commit -m "feat: l'interop de jeu 3D et son composant

Le clic 3D remonte en C# par DotNetObjectReference et déclenche le même
FireAsync qu'avant. dispose remet frozen à false et retire l'écouteur
resize ; _disposed est posé avant le désabonnement et testé après chaque
await — deux défauts livrés lors d'un chantier précédent.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 5 : la grille DOM devient la couche d'annonce

**Files:**
- Modify: `BattleShip.App/Components/FiringGrid.razor`
- Modify: `BattleShip.App/wwwroot/css/app.css`
- Modify: `BattleShip.App/Pages/Play.razor`
- Delete: `BattleShip.App/wwwroot/js/scene.js`

**Interfaces:**
- Consumes: `GameCanvas` (tâche 4), `ScenePlan.For` (tâche 1)
- Produces: `FiringGrid` accepte `[Parameter] public bool Silent { get; set; }` — quand il est vrai, la grille porte la classe `grid--silent`

- [ ] **Étape 1 : ajouter le mode silencieux à `FiringGrid`**

Ajouter le paramètre :

```csharp
    [Parameter] public bool Silent { get; set; }
```

et sur le `<div class="board-frame">` — l'élément **extérieur**, ligne 3 du fichier, pas le `.board` qu'il contient :

```razor
<div class="board-frame @(Silent ? "board-frame--silent" : null)">
```

**Rien d'autre ne change.** Les boutons gardent `aria-label`, `tabindex`, `@onkeydown`, `@onfocus` — c'est tout l'intérêt : la couche d'annonce **est** la grille existante.

- [ ] **Étape 2 : effacer visuellement la grille silencieuse**

À la fin de `app.css` :

```css
.board-frame--silent {
    position: absolute;
    inset: 0;
    opacity: 0;
}

.board-frame--silent .cell:focus-visible {
    opacity: 1;
    outline: 3px solid var(--accent);
    outline-offset: -3px;
}

.game-canvas {
    display: block;
    width: 100%;
    height: clamp(360px, 62vh, 760px);
    border-radius: var(--radius-panel);
}

.stage--3d { position: relative; }
```

**`opacity: 0` et non `display: none`** : un élément en `display: none` sort de l'ordre de tabulation et disparaît de l'arbre d'accessibilité. Il resterait focalisable en `visibility: hidden` ? Non — lui aussi le retire. `opacity: 0` le garde focalisable et annonçable, ce qui est exactement ce qu'on veut. La règle `:focus-visible` rend le contour visible pour un utilisateur voyant au clavier.

- [ ] **Étape 3 : câbler dans `Play.razor`**

Supprimer la ligne 32, `<SceneCanvas State="@_scene" />`.

Puis, dans le bloc `<div class="stage">`, remplacer l'ouverture `<div class="stage__board">` (ligne 53) et la `<FiringGrid>` du plateau adverse qu'elle contient par :

```razor
        <div class="stage__board stage--3d">
            <GameCanvas State="@_scene" OnPick="FireAsync" Focused="@_focusedCell" />
            <FiringGrid @ref="_opponentGrid"
                        GridSize="game.Opponent.GridSize"
                        Cells="OpponentCells(game)"
                        Ships="OpponentHulls(game)"
                        Label="Opponent board — choose a square to fire at"
                        Interactive="@(!finished && !_firing && _replayCursor is null)"
                        Impact="_opponentImpact"
                        SunkLabel="@_sunkLabel"
                        OnCellClick="FireAsync"
                        OnCellFocus="c => _focusedCell = c"
                        Silent="true" />
```

Le `<p class="board-caption">` qui suit et la fermeture du `div` ne bougent pas. La seconde `FiringGrid` — le plateau du joueur, ligne 76 — **ne change pas non plus** : elle reste en 2D dans ce sous-chantier.

Ajouter le champ et le paramètre de focus. Dans `FiringGrid`, exposer le focus :

```csharp
    [Parameter] public EventCallback<Coordinate> OnCellFocus { get; set; }
```

et dans le `@onfocus` du bouton :

```razor
                            @onfocus="() => { _focused = index; OnCellFocus.InvokeAsync(cell); }"
```

Dans `Play.razor`, ajouter :

```csharp
    private Coordinate? _focusedCell;
```

Retirer le `<SceneCanvas ... />` et le composant `SceneCanvas.razor` n'est plus référencé — **supprime aussi `BattleShip.App/Components/SceneCanvas.razor` et `BattleShip.App/wwwroot/js/scene.js`**.

- [ ] **Étape 4 : vérifier**

Run: `dotnet build` — Expected: **0 avertissement**.
Run: `dotnet test` — Expected: inchangé.
Run: `grep -rn "SceneCanvas\|js/scene.js" BattleShip.App/` — Expected: **aucune ligne**.

- [ ] **Étape 5 : commit**

```bash
dotnet format
git add -A BattleShip.App/
git commit -m "feat: la grille DOM devient la couche d'annonce de la scène 3D

Les 100 boutons restent, avec leurs aria-label, tabindex roving et
flèches : ils sont simplement effacés visuellement. opacity: 0 et non
display: none ni visibility: hidden, qui les retireraient tous deux de
l'arbre d'accessibilité et de l'ordre de tabulation.

Le focus clavier éclaire la case 3D correspondante. L'ancien décor et
son composant sont supprimés.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Tâche 6 : vérification à l'écran

Aucun test ne couvre le rendu ni l'interaction. Cette tâche produit les **preuves**.

**Files:** aucun

- [ ] **Étape 1 : serveurs FRAÎCHEMENT relancés**

```bash
pkill -f "dotnet run --project BattleShip" || true
sleep 3
dotnet run --project BattleShip.API &
dotnet run --project BattleShip.App &
```

**Obligatoire** : éditer `wwwroot/` pendant que `dotnet run` tourne rend les empreintes d'assets périmées, et le module n'est alors **jamais évalué, sans erreur en console**. Un chantier précédent a passé trois relectures avant de s'en apercevoir.

- [ ] **Étape 2 : confirmer que le module est chargé AVANT toute mesure**

Dans la console :

```js
typeof window.battleshipGame3d
performance.getEntriesByType('resource').filter(e => /three\.module/.test(e.name)).length
```

Expected : `"object"` et `1`. **Si ce n'est pas le cas, relance les serveurs et recommence** — toute mesure faite dans cet état serait fausse.

> La spec § 5 écrit `window.battleshipScene` : c'est l'ancien nom, hérité du chantier décoratif. L'objet livré ici s'appelle `battleshipGame3d`. C'est le nom du plan qui fait foi.

- [ ] **Étape 3 : énoncer les résultats attendus AVANT de jouer**

Écrire dans le rapport, **avant** d'exécuter :

1. survoler une case du plateau adverse l'éclaire ; sortir du canvas éteint le surlignage ;
2. cliquer une case tire sur **cette** case — à confirmer contre le serveur, pas à l'œil ;
3. `Tab` puis les flèches parcourent les cases ; la case 3D correspondante s'éclaire ; un contour est visible ;
4. un lecteur d'écran annonce les cases ;
5. la scène tient de 360 px de large à un grand écran ;
6. le réglage « Impacts calme » fige la scène sans rechargement.

- [ ] **Étape 4 : jouer les six scénarios et consigner l'observé**

Pour le point 2, comparer la case cliquée au dernier `ShotFired` du serveur :

```js
const id = localStorage.getItem('battleship.currentGame');
const ev = await (await fetch(`http://localhost:5184/games/${id}/events`)).json();
ev.filter(e => e.type === 'shotFired' && e.by === 'Human').at(-1)
```

- [ ] **Étape 5 : le lecteur d'écran, pour de vrai**

Le `README.md` admet depuis le début qu'**aucun lecteur d'écran réel n'a jamais été essayé**. Ce chantier le fait : lance Orca (`orca &` sous GNOME) ou NVDA, parcours la grille au clavier, et consigne ce qui est **réellement annoncé**, mot pour mot. Si ce n'est pas exploitable, dis-le — c'est une information plus utile qu'un succès annoncé.

- [ ] **Étape 6 : consigner**

Rapport selon les trois registres du `CLAUDE.md` § 6.4 : **constaté**, **supposé**, **reste à vérifier**. Aucun « ça marche » sans la manipulation et son résultat.

---

## Tâche 7 : les livrables

**Files:** Modify `PROMPTS.md`, `REVUE-IA.md`, `README.md`, `docs/conformite.md`

- [ ] **Étape 1 : la revue dans `REVUE-IA.md`**

Sujet : **le calque d'annonce**. Gabarit du `CLAUDE.md` § 5.3.

- *Hypothèse* : un jeu rendu en `<canvas>` peut conserver l'intégralité de l'accessibilité livrée, sans réimplémentation, en gardant les boutons comme couche d'annonce.
- *Pouvoir discriminant* : le test est un **lecteur d'écran réel**, pas une lecture de code — et c'est la première fois du projet.
- *Avant / après* : l'ADR 0012 tenait qu'un jeu en canvas coûtait l'accessibilité. Consigner ce qui a été mesuré.
- *Limites* : un seul lecteur d'écran, un seul navigateur, un seul système.

- [ ] **Étape 2 : `PROMPTS.md`**

Une entrée, format condensé des entrées existantes. Y consigner le renversement de l'ADR 0012 et ce qui l'a rendu possible.

- [ ] **Étape 3 : `README.md`**

- Fonctionnalités : le plateau se joue en 3D ; le clavier et les lecteurs d'écran passent par la grille conservée.
- **Retirer** la limite « aucun lecteur d'écran réel n'a été essayé » **si et seulement si** la tâche 6 l'a réellement fait — sinon la conserver telle quelle.
- Ajouter les limites observées : ce qui n'a pas été vérifié, les navigateurs non testés.

- [ ] **Étape 4 : `docs/conformite.md`**

Une ligne par exigence, **une commande par exigence**. **Exécute chaque commande** et écris le nombre observé. Vérifie aussi les compteurs existants : deux avaient dérivé au chantier précédent, dont un hérité de l'avant-dernier.

- [ ] **Étape 5 : vérification finale**

```bash
dotnet build
dotnet test
dotnet format --verify-no-changes
grep -rnE "//|/\*" --include=*.cs --include=*.razor BattleShip.Models BattleShip.API BattleShip.App | grep -v "https://"
```

La dernière commande ne doit rien rendre.

- [ ] **Étape 6 : commit**

```bash
git add PROMPTS.md REVUE-IA.md README.md docs/conformite.md
git commit -m "docs: consigne le passage du jeu en 3D et la revue du calque d'annonce

La revue porte sur l'hypothèse qui a permis de renverser l'ADR 0012 :
garder les boutons comme couche d'annonce plutôt que réimplémenter
l'accessibilité. Elle est éprouvée par un lecteur d'écran réel — une
première sur ce projet.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Auto-revue du plan

**1. Couverture de la spec**

| section de la spec | tâche |
|---|---|
| § 1 ce qui change / est jeté | 0, 5 |
| § 1 ADR 0012 remplacé | 0 |
| § 2 le calque d'annonce | 5 |
| § 2 approches écartées | 0 (ADR) |
| § 2 le voyant au clavier | 4 (`hover`), 5 (`:focus-visible`) |
| § 3 scène, tir, caméra | 2, 3 |
| § 3 découpage du module | 2, 3, 4 |
| § 4 `SceneState` étendu | 1 |
| § 4 test de non-fuite générique | 1 |
| § 5 tests C# | 1 |
| § 5 protocole écran | 6 |
| § 5 lecteur d'écran réel | 6 étape 5 |
| § 5 précaution empreintes | 6 étapes 1-2 |
| § 6 hors périmètre | 0 (ADR) |

Aucune section sans tâche.

**2. Placeholders** — aucun `TBD`, aucun « gérer les erreurs », aucun « comme la tâche N ». Le code est écrit en entier.

**3. Cohérence des types** — `SceneCell(int X, int Y, string State)` défini en tâche 1, consommé en tâche 2 (`cell.x`, `cell.y`, `cell.state` — camelCase côté JS, la sérialisation Blazor l'assure). `createScene` rend `{ scene, boards, setState, dispose }`, consommé tel quel en tâche 4. `createInput(renderer, camera, getBoards, onPick)` — noter que `getBoards` est une **fonction**, parce que `boards` n'existe pas encore au moment où `createInput` est appelé. `battleshipGame3d` expose cinq fonctions, toutes appelées par `GameCanvas`.

**Trois points à relire avec attention pendant l'exécution :**

- **Tâche 1** — les états de case doivent venir **du journal**, pas de `game.Opponent.Shots`, sinon ils ignorent le curseur de rejeu. Le second test l'attrape.
- **Tâche 3** — les trois écouteurs de `input.js` doivent être retirés par `dispose`. Un écouteur oublié a déjà coûté une fuite **et** un plantage sur ce projet.
- **Tâche 5** — `opacity: 0` et rien d'autre. `display: none` et `visibility: hidden` retirent l'élément de l'arbre d'accessibilité **et** de l'ordre de tabulation, ce qui annulerait tout l'intérêt du calque.
